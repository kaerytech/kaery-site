const translations = {
  pt: {
    htmlLang: "pt-BR",
    title: "Kaery | Soluções Digitais para Empresas",
    description:
      "A Kaery cria sites, landing pages, automações e estratégias digitais para empresas que desejam crescer com tecnologia e performance.",
    nav: ["Início", "Sobre", "Serviços", "Portfólio", "Processo", "Contato"],
    cta: "Falar no WhatsApp",
    heroEyebrow: "Kaery | kaery.com.br",
    heroTitle: "Soluções digitais para empresas que querem crescer.",
    heroSubtitle:
      "A Kaery cria sites, landing pages, automações e estratégias digitais para transformar presença online em resultado.",
    heroSecondary: "Conhecer serviços",
    heroLines: ["Código", "Interface", "Sistema"],
    badges: ["WEB Sites Profissionais", "Landing Pages", "AUTO Automações", "Google Ads"],
    aboutEyebrow: "QUEM SOMOS",
    aboutTitle: "Transformamos ideias em soluções digitais.",
    aboutParagraphs: [
      "A Kaery nasceu para ajudar empresas a crescer por meio da tecnologia. Desenvolvemos sites, landing pages, automações e soluções digitais que unem design, performance e estratégia.",
      "Mais do que criar páginas, construímos experiências que fortalecem marcas, aumentam a presença online e geram oportunidades de negócio.",
    ],
    pillars: ["Estratégia", "Design", "Desenvolvimento", "Performance"],
    servicesEyebrow: "O QUE FAZEMOS",
    servicesTitle: "Soluções que impulsionam empresas no ambiente digital.",
    servicesDescription:
      "Desenvolvemos soluções digitais completas para empresas que desejam crescer, vender mais e fortalecer sua presença online com tecnologia e estratégia.",
    serviceControls: ["Anterior", "Próximo serviço"],
    services: [
      ["Sites Institucionais", "Sites profissionais para apresentar sua empresa com autoridade e clareza."],
      ["Landing Pages", "Páginas focadas em campanhas, captação de leads e conversão."],
      ["Automações", "Fluxos inteligentes para reduzir tarefas repetitivas e ganhar eficiência."],
      ["Google Ads", "Estratégias para atrair clientes certos e gerar oportunidades de venda."],
      ["Identidade Visual", "Direção visual para fortalecer a percepção e a consistência da marca."],
      ["Desenvolvimento Web", "Soluções web sob medida, preparadas para crescer junto com o negócio."],
    ],
    portfolioEyebrow: "PORTFÓLIO",
    portfolioTitle: "Projetos desenvolvidos para gerar resultados.",
    portfolioDescription:
      "Cada projeto é desenvolvido com foco em performance, experiência do usuário e resultados para o negócio.",
    projects: [
      ["Landing Page", "Landing Page Empresarial", "Página comercial criada para apresentar uma oferta com clareza e aumentar conversões."],
      ["Website", "Site Institucional", "Presença digital profissional para fortalecer autoridade e gerar confiança."],
      ["Sistema", "Sistema Web", "Solução web planejada para organizar processos e apoiar a operação do negócio."],
    ],
    projectButton: "Ver projeto",
    processEyebrow: "COMO TRABALHAMOS",
    processTitle: "Um processo simples, transparente e focado em resultados.",
    processDescription:
      "Acreditamos que grandes projetos nascem de uma boa comunicação. Nosso processo foi pensado para oferecer clareza, agilidade e qualidade em todas as etapas.",
    steps: [
      ["Planejamento", "Entendimento do negócio, objetivos e necessidades."],
      ["Design", "Criação da interface focada na experiência do usuário."],
      ["Desenvolvimento", "Transformação do projeto em uma solução funcional e responsiva."],
      ["Entrega", "Publicação, testes finais e acompanhamento inicial."],
    ],
    contactEyebrow: "VAMOS CONVERSAR",
    contactTitle: "Seu próximo projeto começa com uma boa conversa.",
    contactDescription:
      "Estamos prontos para entender o seu desafio e desenvolver uma solução digital sob medida para o seu negócio. Conheça planos e personalizações pensadas para fortalecer sua presença digital com estratégia.",
    contactLabels: ["WhatsApp", "E-mail", "Site", "Atendimento"],
    location: "Todo Brasil e exterior",
    fields: ["Nome", "E-mail", "Telefone", "Empresa", "Mensagem"],
    submit: "Enviar Mensagem",
    successTitle: "Mensagem enviada com sucesso.",
    successText: "Recebemos suas informações e vamos retornar em breve pelo canal informado.",
    duplicateText: "Seu contato já foi encaminhado. Para enviar novamente, altere o e-mail ou telefone.",
    footerText: "Soluções digitais para empresas que desejam crescer com tecnologia, estratégia e inovação.",
    footerNavTitle: "Navegação",
    footerContactTitle: "Contato",
    footerSocialTitle: "Redes Sociais",
    copyright: "© 2026 Kaery. Todos os direitos reservados.",
    developed: "Desenvolvido por Kaery.",
  },
  en: {
    htmlLang: "en",
    title: "Kaery | Digital Solutions for Companies",
    description:
      "Kaery creates websites, landing pages, automations and digital strategies for companies that want to grow with technology and performance.",
    nav: ["Home", "About", "Services", "Portfolio", "Process", "Contact"],
    cta: "Talk on WhatsApp",
    heroEyebrow: "Kaery | kaery.com.br",
    heroTitle: "Digital solutions for companies ready to grow.",
    heroSubtitle:
      "Kaery creates websites, landing pages, automations and digital strategies that turn online presence into business results.",
    heroSecondary: "Explore services",
    heroLines: ["Code", "Interface", "System"],
    badges: ["WEB Professional Websites", "Landing Pages", "AUTO Automations", "Google Ads"],
    aboutEyebrow: "WHO WE ARE",
    aboutTitle: "We turn ideas into digital solutions.",
    aboutParagraphs: [
      "Kaery was created to help companies grow through technology. We build websites, landing pages, automations and digital solutions that combine design, performance and strategy.",
      "More than creating pages, we build experiences that strengthen brands, expand online presence and generate business opportunities.",
    ],
    pillars: ["Strategy", "Design", "Development", "Performance"],
    servicesEyebrow: "WHAT WE DO",
    servicesTitle: "Solutions that help companies grow online.",
    servicesDescription:
      "We develop complete digital solutions for companies that want to grow, sell more and strengthen their online presence with technology and strategy.",
    serviceControls: ["Previous", "Next service"],
    services: [
      ["Institutional Websites", "Professional websites that present your company with authority and clarity."],
      ["Landing Pages", "Campaign-focused pages built for lead generation and conversion."],
      ["Automations", "Smart workflows that reduce repetitive tasks and improve efficiency."],
      ["Google Ads", "Strategies to attract the right clients and generate sales opportunities."],
      ["Visual Identity", "Visual direction to strengthen brand perception and consistency."],
      ["Web Development", "Custom web solutions prepared to grow with your business."],
    ],
    portfolioEyebrow: "PORTFOLIO",
    portfolioTitle: "Projects built to generate results.",
    portfolioDescription:
      "Every project is developed with a focus on performance, user experience and business outcomes.",
    projects: [
      ["Landing Page", "Business Landing Page", "A commercial page designed to present an offer clearly and increase conversions."],
      ["Website", "Institutional Website", "A professional digital presence built to strengthen authority and trust."],
      ["System", "Web System", "A web solution planned to organize processes and support business operations."],
    ],
    projectButton: "View project",
    processEyebrow: "HOW WE WORK",
    processTitle: "A simple, transparent process focused on results.",
    processDescription:
      "We believe great projects start with clear communication. Our process is designed to bring clarity, agility and quality to every step.",
    steps: [
      ["Planning", "Understanding the business, goals and needs."],
      ["Design", "Interface creation focused on user experience."],
      ["Development", "Turning the project into a functional and responsive solution."],
      ["Delivery", "Publishing, final testing and initial support."],
    ],
    contactEyebrow: "LET'S TALK",
    contactTitle: "Your next project starts with a good conversation.",
    contactDescription:
      "We are ready to understand your challenge and develop a custom digital solution for your business. Explore plans and tailored options designed to strengthen your digital presence with strategy.",
    contactLabels: ["WhatsApp", "Email", "Website", "Service Area"],
    location: "Brazil and international clients",
    fields: ["Name", "Email", "Phone", "Company", "Message"],
    submit: "Send Message",
    successTitle: "Message sent successfully.",
    successText: "We received your information and will reply soon through the contact channel provided.",
    duplicateText: "Your contact was already forwarded. To send again, change the email or phone number.",
    footerText: "Digital solutions for companies that want to grow with technology, strategy and innovation.",
    footerNavTitle: "Navigation",
    footerContactTitle: "Contact",
    footerSocialTitle: "Social Media",
    copyright: "© 2026 Kaery. All rights reserved.",
    developed: "Developed by Kaery.",
  },
};

const setText = (selector, value) => {
  const element = document.querySelector(selector);
  if (element) element.textContent = value;
};

const setMany = (selector, values, updater) => {
  document.querySelectorAll(selector).forEach((element, index) => {
    if (values[index]) updater(element, values[index], index);
  });
};

const applyLanguage = (language) => {
  const dictionary = translations[language] || translations.pt;

  document.documentElement.lang = dictionary.htmlLang;
  document.title = dictionary.title;
  document.querySelector('meta[name="description"]')?.setAttribute("content", dictionary.description);
  document.querySelector('meta[property="og:title"]')?.setAttribute("content", dictionary.title);
  document.querySelector('meta[property="og:description"]')?.setAttribute("content", dictionary.description);
  document.querySelector('meta[name="twitter:title"]')?.setAttribute("content", dictionary.title);
  document.querySelector('meta[name="twitter:description"]')?.setAttribute("content", dictionary.description);

  setMany(".navbar__link", dictionary.nav, (element, value) => { element.textContent = value; });
  setText(".navbar__cta", dictionary.cta);
  setText(".hero__eyebrow", dictionary.heroEyebrow);
  setText(".hero__title", dictionary.heroTitle);
  setText(".hero__subtitle", dictionary.heroSubtitle);
  setText(".hero__button--primary", dictionary.cta);
  setText(".hero__button--secondary", dictionary.heroSecondary);
  setMany(".hero__imac-caption", dictionary.heroLines, (element, value) => { element.textContent = value; });
  setMany(".hero__badge", dictionary.badges, (element, value) => { element.textContent = value; });

  setText(".about__eyebrow", dictionary.aboutEyebrow);
  setText(".about__title", dictionary.aboutTitle);
  setMany(".about__text p", dictionary.aboutParagraphs, (element, value) => { element.textContent = value; });
  setMany(".about__pillar h3", dictionary.pillars, (element, value) => { element.textContent = value; });

  setText(".services__eyebrow", dictionary.servicesEyebrow);
  setText(".services__title", dictionary.servicesTitle);
  setText(".services__description", dictionary.servicesDescription);
  setText('[data-service-control="prev"]', dictionary.serviceControls[0]);
  setText('[data-service-control="next"]', dictionary.serviceControls[1]);
  document.querySelector('[data-service-control="prev"]')?.setAttribute("aria-label", dictionary.serviceControls[0]);
  document.querySelector('[data-service-control="next"]')?.setAttribute("aria-label", dictionary.serviceControls[1]);
  setMany(".services__showcase-slide", dictionary.services, (element, value, index) => {
    element.querySelector(".services__showcase-content span").textContent = String(index + 1).padStart(2, "0");
    element.querySelector("h3").textContent = value[0];
    element.querySelector("p").textContent = value[1];
  });
  setText(".portfolio__eyebrow", dictionary.portfolioEyebrow);
  setText(".portfolio__title", dictionary.portfolioTitle);
  setText(".portfolio__description", dictionary.portfolioDescription);
  setMany(".portfolio__card", dictionary.projects, (element, value) => {
    element.querySelector(".portfolio__category").textContent = value[0];
    element.querySelector("h3").textContent = value[1];
    element.querySelector(".portfolio__content p:not(.portfolio__category)").textContent = value[2];
    element.querySelector(".portfolio__button").textContent = dictionary.projectButton;
  });

  setText(".process__eyebrow", dictionary.processEyebrow);
  setText(".process__title", dictionary.processTitle);
  setText(".process__description", dictionary.processDescription);
  setMany(".process__step", dictionary.steps, (element, value) => {
    element.querySelector("h3").textContent = value[0];
    element.querySelector("p").textContent = value[1];
  });

  setText(".contact__eyebrow", dictionary.contactEyebrow);
  setText(".contact__title", dictionary.contactTitle);
  setText(".contact__description", dictionary.contactDescription);
  setMany(".contact__label", dictionary.contactLabels, (element, value) => { element.textContent = value; });
  setText(".contact__info:nth-of-type(4) p:not(.contact__label)", dictionary.location);
  setText(".contact__cta", dictionary.cta);
  setMany(".contact__field label", dictionary.fields, (element, value) => { element.textContent = value; });
  setText(".contact__submit", dictionary.submit);
  setText(".contact__success-title", dictionary.successTitle);
  setText(".contact__success-text", dictionary.successText);
  setText(".contact__duplicate", dictionary.duplicateText);
  setText(".contact__social h3", dictionary.footerSocialTitle);

  setText(".site-footer__brand p", dictionary.footerText);
  setText(".site-footer__nav h2", dictionary.footerNavTitle);
  setMany(".site-footer__nav a", dictionary.nav, (element, value) => { element.textContent = value; });
  setText(".site-footer__contact h2", dictionary.footerContactTitle);
  setText(".site-footer__social h2", dictionary.footerSocialTitle);
  setText(".site-footer__contact li:nth-child(4) span:last-child", dictionary.location);
  setText(".site-footer__bottom p:first-child", dictionary.copyright);
  setText(".site-footer__bottom p:last-child", dictionary.developed);

  document.querySelectorAll(".navbar__lang-button").forEach((button) => {
    const isActive = button.dataset.lang === language;
    button.classList.toggle("is-active", isActive);
    button.setAttribute("aria-pressed", String(isActive));
  });

  localStorage.setItem("kaery-language", language);
};

document.querySelectorAll(".navbar__lang-button").forEach((button) => {
  button.addEventListener("click", () => applyLanguage(button.dataset.lang));
});

applyLanguage(localStorage.getItem("kaery-language") || "pt");

const getContactSignature = (form) => {
  const email = form.querySelector("#email")?.value.trim().toLowerCase() || "";
  const phone = form.querySelector("#telefone")?.value.replace(/\D/g, "") || "";
  return `${email}|${phone}`;
};

const showDuplicateContactMessage = (form) => {
  const language = localStorage.getItem("kaery-language") || "pt";
  const dictionary = translations[language] || translations.pt;
  let message = form.querySelector(".contact__duplicate");

  if (!message) {
    message = document.createElement("p");
    message.className = "contact__duplicate";
    message.setAttribute("role", "status");
    form.prepend(message);
  }

  message.textContent = dictionary.duplicateText;
};

const hideDuplicateContactMessage = (form) => {
  form.querySelector(".contact__duplicate")?.remove();
};

const setupContactDuplicateGuard = () => {
  const form = document.querySelector(".contact__form");
  if (!form) return;

  form.querySelectorAll("#email, #telefone").forEach((field) => {
    field.addEventListener("input", () => {
      const signature = getContactSignature(form);
      const lastSignature = localStorage.getItem("kaery-last-contact-signature");

      if (signature !== lastSignature) {
        hideDuplicateContactMessage(form);
      }
    });
  });

  form.addEventListener("submit", (event) => {
    const signature = getContactSignature(form);
    const lastSignature = localStorage.getItem("kaery-last-contact-signature");

    if (signature && signature === lastSignature) {
      event.preventDefault();
      showDuplicateContactMessage(form);
      return;
    }

    localStorage.setItem("kaery-pending-contact-signature", signature);
  });
};

setupContactDuplicateGuard();

const showContactSuccess = () => {
  const params = new URLSearchParams(window.location.search);
  const form = document.querySelector(".contact__form");

  if (params.get("sent") !== "1" || !form) return;

  const pendingSignature = localStorage.getItem("kaery-pending-contact-signature");
  if (pendingSignature) {
    localStorage.setItem("kaery-last-contact-signature", pendingSignature);
    localStorage.removeItem("kaery-pending-contact-signature");
  }

  form.innerHTML = `
    <div class="contact__success" role="status" aria-live="polite">
      <span class="contact__success-mark" aria-hidden="true">OK</span>
      <h3 class="contact__success-title"></h3>
      <p class="contact__success-text"></p>
      <a class="contact__success-link" href="https://wa.me/5573999938297">Falar no WhatsApp</a>
    </div>
  `;

  const language = localStorage.getItem("kaery-language") || "pt";
  const dictionary = translations[language] || translations.pt;
  setText(".contact__success-title", dictionary.successTitle);
  setText(".contact__success-text", dictionary.successText);
  setText(".contact__success-link", dictionary.cta);

  window.history.replaceState({}, document.title, `${window.location.pathname}#contato`);
};

showContactSuccess();

const imacImages = document.querySelectorAll(".hero__imac-image");
const imacDots = document.querySelectorAll(".hero__imac-dot");
let imacCurrentSlide = 0;

const showImacSlide = (index) => {
  const nextSlide = (index + imacImages.length) % imacImages.length;
  if (nextSlide === imacCurrentSlide) return;

  imacImages.forEach((image, imageIndex) => {
    image.classList.remove("is-active", "is-leaving", "is-before");
    if (imageIndex === imacCurrentSlide) image.classList.add("is-leaving");
    if (imageIndex === nextSlide) image.classList.add("is-active");
    if (imageIndex !== imacCurrentSlide && imageIndex !== nextSlide) image.classList.add("is-before");
  });

  imacCurrentSlide = nextSlide;

  imacDots.forEach((dot, dotIndex) => {
    dot.classList.toggle("is-active", dotIndex === imacCurrentSlide);
  });
};

if (imacImages.length > 0 && imacDots.length > 0) {
  imacDots.forEach((dot) => {
    dot.addEventListener("click", () => showImacSlide(Number(dot.dataset.slide)));
  });

  window.setInterval(() => {
    showImacSlide(imacCurrentSlide + 1);
  }, 4200);
}

const serviceSlides = document.querySelectorAll(".services__showcase-slide");
const serviceDots = document.querySelectorAll(".services__tv-dot");
const servicePrev = document.querySelector('[data-service-control="prev"]');
const serviceNext = document.querySelector('[data-service-control="next"]');
let serviceCurrentSlide = 0;
let serviceTimer = null;

const showServiceSlide = (index) => {
  if (serviceSlides.length === 0) return;

  const nextSlide = (index + serviceSlides.length) % serviceSlides.length;
  if (nextSlide === serviceCurrentSlide) return;

  serviceSlides.forEach((slide, slideIndex) => {
    slide.classList.remove("is-active", "is-leaving");
    if (slideIndex === serviceCurrentSlide) slide.classList.add("is-leaving");
    if (slideIndex === nextSlide) slide.classList.add("is-active");
  });

  serviceCurrentSlide = nextSlide;

  serviceDots.forEach((dot, dotIndex) => {
    dot.classList.toggle("is-active", dotIndex === serviceCurrentSlide);
  });
};

const restartServiceTimer = () => {
  window.clearInterval(serviceTimer);
  serviceTimer = window.setInterval(() => {
    showServiceSlide(serviceCurrentSlide + 1);
  }, 4800);
};

if (serviceSlides.length > 0) {
  servicePrev?.addEventListener("click", () => {
    showServiceSlide(serviceCurrentSlide - 1);
    restartServiceTimer();
  });

  serviceNext?.addEventListener("click", () => {
    showServiceSlide(serviceCurrentSlide + 1);
    restartServiceTimer();
  });

  restartServiceTimer();
}

const initStarField = (starCanvas, density = 1) => {
  const context = starCanvas.getContext("2d", { alpha: true });
  const section = starCanvas.closest(".hero, .contact");
  const reduceMotion = window.matchMedia("(prefers-reduced-motion: reduce)");
  const pointer = { x: 0, y: 0 };
  const stars = [];
  let width = 0;
  let height = 0;
  let pixelRatio = 1;
  let animationFrame = null;
  let isVisible = true;

  const randomBetween = (min, max) => Math.random() * (max - min) + min;

  const createStar = (layer) => {
    const settings = {
      far: { radius: [0.25, 0.75], speed: [0.003, 0.012], alpha: [0.25, 0.6] },
      mid: { radius: [0.65, 1.25], speed: [0.018, 0.038], alpha: [0.38, 0.78] },
      near: { radius: [1.1, 1.9], speed: [0.045, 0.085], alpha: [0.55, 0.95] },
    }[layer];

    return {
      x: Math.random() * width,
      y: Math.random() * height,
      z: randomBetween(0.2, 1),
      radius: randomBetween(settings.radius[0], settings.radius[1]),
      speed: randomBetween(settings.speed[0], settings.speed[1]),
      alpha: randomBetween(settings.alpha[0], settings.alpha[1]),
      twinkle: layer === "near" || Math.random() > 0.86,
      phase: Math.random() * Math.PI * 2,
      layer,
    };
  };

  const buildStars = () => {
    stars.length = 0;
    const area = width * height;
    const mobileFactor = width < 768 ? 0.56 : 1;
    const baseTotal = Math.floor(area / 1050) * mobileFactor * density;
    const minStars = Math.floor(260 * density);
    const maxStars = Math.floor(1300 * density);
    const total = Math.floor(Math.min(maxStars, Math.max(minStars, baseTotal)));
    const farCount = Math.floor(total * 0.9);
    const midCount = Math.floor(total * 0.09);
    const nearCount = Math.max(Math.floor(6 * density), Math.min(Math.floor(15 * density), total - farCount - midCount));

    for (let index = 0; index < farCount; index += 1) stars.push(createStar("far"));
    for (let index = 0; index < midCount; index += 1) stars.push(createStar("mid"));
    for (let index = 0; index < nearCount; index += 1) stars.push(createStar("near"));
  };

  const resize = () => {
    const rect = starCanvas.getBoundingClientRect();
    width = Math.max(1, Math.floor(rect.width));
    height = Math.max(1, Math.floor(rect.height));
    pixelRatio = Math.min(window.devicePixelRatio || 1, 2);
    starCanvas.width = Math.floor(width * pixelRatio);
    starCanvas.height = Math.floor(height * pixelRatio);
    context.setTransform(pixelRatio, 0, 0, pixelRatio, 0, 0);
    buildStars();
  };

  const render = (time = 0) => {
    context.clearRect(0, 0, width, height);
    context.fillStyle = "rgba(2, 3, 8, 0.34)";
    context.fillRect(0, 0, width, height);

    const centerX = width / 2;
    const centerY = height / 2;
    const driftX = pointer.x * 2;
    const driftY = pointer.y * 2;

    for (const star of stars) {
      if (!reduceMotion.matches) {
        star.x += (star.x - centerX) * star.speed * 0.0018;
        star.y += (star.y - centerY) * star.speed * 0.0018;
        star.z += star.speed * 0.0012;
      }

      if (star.x < -20 || star.x > width + 20 || star.y < -20 || star.y > height + 20 || star.z > 1.35) {
        const next = createStar(star.layer);
        star.x = centerX + randomBetween(-18, 18);
        star.y = centerY + randomBetween(-18, 18);
        star.z = 0.2;
        star.radius = next.radius;
        star.speed = next.speed;
        star.alpha = next.alpha;
        star.twinkle = next.twinkle;
        star.phase = next.phase;
      }

      const depth = star.layer === "far" ? 0.25 : star.layer === "mid" ? 0.58 : 1;
      const x = star.x + (reduceMotion.matches ? 0 : driftX * depth);
      const y = star.y + (reduceMotion.matches ? 0 : driftY * depth);
      const pulse = star.twinkle && !reduceMotion.matches ? Math.sin(time * 0.0012 + star.phase) * 0.22 : 0;
      const alpha = Math.max(0.14, Math.min(1, star.alpha + pulse));
      const radius = star.radius * (0.8 + star.z * 0.45);

      context.beginPath();
      context.fillStyle = `rgba(165, 226, 255, ${alpha})`;
      context.shadowBlur = star.layer === "near" ? 10 : star.layer === "mid" ? 5 : 0;
      context.shadowColor = "rgba(56, 189, 248, 0.55)";
      context.arc(x, y, radius, 0, Math.PI * 2);
      context.fill();
    }

    context.shadowBlur = 0;
    if (isVisible && !reduceMotion.matches) animationFrame = window.requestAnimationFrame(render);
  };

  const start = () => {
    if (!animationFrame) animationFrame = window.requestAnimationFrame(render);
  };

  const stop = () => {
    window.cancelAnimationFrame(animationFrame);
    animationFrame = null;
  };

  window.addEventListener("resize", resize);
  section.addEventListener("pointermove", (event) => {
    const rect = section.getBoundingClientRect();
    pointer.x = ((event.clientX - rect.left) / rect.width - 0.5) * 2;
    pointer.y = ((event.clientY - rect.top) / rect.height - 0.5) * 2;
  });

  section.addEventListener("pointerleave", () => {
    pointer.x = 0;
    pointer.y = 0;
  });

  const observer = new IntersectionObserver(([entry]) => {
    isVisible = entry.isIntersecting;
    if (isVisible) {
      start();
      return;
    }
    stop();
  });

  resize();
  start();
  observer.observe(section);
};

document.querySelectorAll(".hero__stars, .contact__stars").forEach((canvas) => {
  const density = canvas.classList.contains("contact__stars") ? 0.36 : 1;
  initStarField(canvas, density);
});
